# PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/haber_sitesi"

# Auth.js
AUTH_SECRET="generate-with-npx-auth-secret"

# App URL (used in emails and for absolute links)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Vercel Blob (image uploads) — required for the /api/upload route
BLOB_READ_WRITE_TOKEN=""
